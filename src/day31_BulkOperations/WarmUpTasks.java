package day31_BulkOperations;
/*
Write a program that can move all the zeros to  last indexes of ArrayList
		Ex:
			list:  {1,0,2,0,3,0,4,0};
			output: [1, 2, 3, 4, 0, 0, 0, 0]
			DO NOT USE SORT method, DO NOT declear  any extra lists
	write a program that can return the duplicated values from an ArrayList of String
		Ex:
			list: {"A", "B", "A", "C", "D"};
			output: ["A"]
			list: {"A", "B", "B", "C", "D", "D"};
			output: ["A", "C"]
 */


import java.util.ArrayList;
import java.util.Arrays;

public class WarmUpTasks {
    public static void main(String[] args) {
/*
        ArrayList<Integer> list = new ArrayList<>();
        Integer[] ab = {1, 0, 2, 0, 3, 0, 4, 0};
        list.addAll(Arrays.asList(ab));

        System.out.println(list);

        for (int i = 0; i < list.size(); i++) {
            if (list.get(i).equals(0)) ;
            list.remove(list.get(i));
            list.add(0);
        }
    }

        System.out.println(list);


        System.out.println("=====================================");

        String at[] = {"A", "B", "B", "C", "D", "D"};

        ArrayList<String> atlar = new ArrayList<>();
         atlar.addAll(Arrays.asList(at));


     //   System.out.println(atlar);
        for (String each : atlar) {
            int count = 0;

            for (String each2 : atlar) {
                if (each == each2) {
                    count++;
                }
            }
                if (count==1) {
                   atlar.add(each);


            }

        }
        System.out.println();
            System.out.println(atlar);


 */
    }
}
